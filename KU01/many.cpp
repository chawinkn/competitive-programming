#include <bits/stdc++.h>
using namespace std;

int arr[100001];
int main() {
  int n, k;
  long long int cnt=0;
  cin >> n >> k;
  for (int i = 0; i < n; i++) cin >> arr[i];
  for (int i = 0; i < n; i++) {
    for (int j = 0; j < n; j++) {
      
    }
  }
  cout << cnt;
  return 0;
}